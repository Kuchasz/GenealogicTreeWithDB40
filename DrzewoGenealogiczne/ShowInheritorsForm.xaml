<Window x:Class="GeoTree.UI.ShowInheritorsForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ShowInheritorsForm" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DataGrid Margin="5" ItemsSource="{Binding}" AutoGenerateColumns="True" IsReadOnly="True"></DataGrid>
        <Button Margin="5" Padding="5" Grid.Row="1" Click="Button_Click">Zamknij</Button>
    </Grid>
</Window>
